version: "3.8"
services:
  mariadb:
    image: mariadb:10.8
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --skip-character-set-client-handshake
      - --skip-innodb-read-only-compressed
    environment:
      MYSQL_ROOT_PASSWORD: 123
    volumes:
      - mariadb-data:/var/lib/mysql

  redis:
    image: redis:alpine

  frappe:
    build: .
    ports:
<<<<<<< HEAD
      - 8100:8000
      - 9100:9000
=======
      - 8000:8000
      - 9000:9000
    environment:
      - SHELL=/bin/bash
    depends_on:
      - mariadb
      - redis
>>>>>>> 7f9a00ac0 (Fix: bake init.sh inside Docker image for Coolify)

volumes:
  mariadb-data:
